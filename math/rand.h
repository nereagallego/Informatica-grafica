#ifndef RAND_HPP
#define RAND_HPP

#include <random>
namespace Rand{
    /**
     * @brief dvuelve un múmero aletatorio de tipo double entre fMin y fMax
     * 
     * @param fMin límite inferior
     * @param fMax límite superior
     * @return double
    */
    double fRand(double fMin,double fMax);
};


#endif